<?php
/*
$to = 'TNLE833@GMAIL.COM';
$subject = test_input($_POST['name']);
$header = test_input($_POST['email']);
$message = $_POST['message'];
$errorMessage = "";

if(empty($subject)){
    $errorMessage = "Name is required";
}
else{
    if(!preg_match("/^[a-zA-Z ]*$/",$subject)){
        $errorMessage = "Only letters and white space allowed";
    }
}

if(empty($header)){
    $errorMessage = "Return email is required";
}
else{
    if (!filter_var($header, FILTER_VALIDATE_EMAIL)) {
        $errorMessage = "Invalid email format";
    }
}

mail($to, $subject, $message, $header);
*/
?>